import { Playfair_Display } from 'next/font/google';

// Editorial serif used on auth pages and brand moments.
export const playfair = Playfair_Display({
  subsets: ['latin'],
  weight: ['500', '600', '700'],
});
